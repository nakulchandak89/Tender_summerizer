const path = require("path");

module.exports = {
  mode: "production",
  entry: {
    "size-webpack-animate": path.join(__dirname, "./lib/animate/index.js"),
  },
  output: {
    filename: "[name].js",
    path: path.resolve(__dirname, "dist"),
    library: {
      type: "module", // Output as ESM
    },
  },
  experiments: {
    outputModule: true, // Enable ESM output
  },
  resolve: {
    extensions: [".wasm", ".mjs", ".js", ".jsx", ".json"],
  },
  externals: {
    react: "react",        // React won't be bundled, assumes global import
    "react-dom": "react-dom",
  },
  module: {
    rules: [
      {
        test: /\.m?js/,
        resolve: {
          fullySpecified: false, // Helps with .mjs imports
        },
      },
    ],
  },
};
